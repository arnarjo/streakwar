# Prototype Files

These are early-stage mock-data prototype files used for design exploration. NOT used in production.

The real production app lives in `src/` and is launched from `App.tsx` (root level).
